import { atom } from 'jotai';

export const isVideoPlayingAtom = atom(true);
export const isVideoMutedAtom = atom(true);
export const isVideoFullscreenAtom = atom(false);
export const isVideoVisibleAtom = atom(false);
